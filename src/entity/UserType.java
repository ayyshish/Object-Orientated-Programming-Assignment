package entity;

/**
 * Enum to tag a user to a specific UserType such as Student, CCM, or Staff
 * Dictates which object/UI page should be used for the User
 */
public enum UserType {
	STUDENT,
	CCM,
	STAFF
}
